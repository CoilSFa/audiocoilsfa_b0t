services:
  - type: web
    name: audiocoilsfa-b0t
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port 10000"
    env: python
    pythonVersion: 3.10.13
    plan: free
